// Tailwind v4 — the PostCSS plugin is the whole setup (no autoprefixer/postcss-import; v4 bundles them).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
